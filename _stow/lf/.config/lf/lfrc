# DOCS: https://github.com/gokcehan/lf/blob/master/doc.md
# EXAMPLE: https://github.com/gokcehan/lf/blob/master/etc/lfrc.example

set hidden
set icons
set number
set previewer ~/bin/lf-previewer
set relativenumber
set shell zsh
set incsearch
set smartcase
set wrapscan false
set mouse false

# use smart previewer for files instead of `less`
map i $~/bin/lf-previewer "$f"

# conformant to my vim config
map <c-q> quit

# https://github.com/gokcehan/lf/wiki/Tips#file-operations

# You can name this ":cdhere" or similar
cmd cdhere %{{
    echo "$PWD" > "$LFCD_FILE"
    $quit
}}
